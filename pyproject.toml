[project]
name = "hermes-hackaton"
version = "0.1.0"
description = "Hermes-backed multi-agent OptimiCity hackathon simulation."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "psycopg[binary]>=3.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
